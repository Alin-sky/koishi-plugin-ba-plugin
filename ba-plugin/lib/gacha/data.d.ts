declare class stu {
    name: string;
    rare: number;
    limit: number;
    server: number;
    constructor(name: string, rare: number, limit: number, server: number);
}
export declare let StudentPool: stu[];
export {};
